interface:
  display_name: "Ship Spool PR"
  short_description: "Create and shepherd a Spool GitHub PR until it is ready"
  default_prompt: "Use $ship-mr to submit the current Spool change as a focused GitHub PR (against main) and shepherd it until ready to merge."
